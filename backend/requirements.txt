# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn==0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
starlette>=0.36.0

# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
joblib>=1.3.0
openpyxl>=3.1.0

# ── LangGraph & LangChain (AI multiagent layer) ───────────────────────────────
langgraph>=1.0.0
langgraph-prebuilt>=1.0.0
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-community>=0.4.0
langchain-text-splitters>=1.0.0

# ── LLM providers ─────────────────────────────────────────────────────────────
openai>=2.0.0

# ── RAG / Vector store ────────────────────────────────────────────────────────
qdrant-client>=1.10.0
langchain-qdrant>=1.0.0

# ── Document loaders ──────────────────────────────────────────────────────────
pypdf>=4.0.0
# unstructured — install separately if needed: pip install unstructured

# ── Observability (optional) ──────────────────────────────────────────────────
# langsmith>=0.8.0
